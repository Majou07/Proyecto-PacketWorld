<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="unidad" >
     <select id="obtener-todas" resultType="pojo.Unidad">
        SELECT
            u.id_unidad        AS idUnidad,
            u.vin,
            u.nii,
            u.marca,
            u.modelo,
            u.anio,
            u.motivo_baja     AS motivoBaja,

            u.id_tipo_unidad  AS idTipoUnidad,
            tu.nombre         AS tipoUnidad,

            u.id_estatus_unidad AS idEstatusUnidad,
            eu.nombre         AS estatusUnidad

        FROM unidad u
        INNER JOIN tipo_unidad tu
            ON u.id_tipo_unidad = tu.id_tipo_unidad
        INNER JOIN estatus_unidad eu
            ON u.id_estatus_unidad = eu.id_estatus_unidad
           
    </select>
    
    <insert id="registrar" parameterType="pojo.Unidad">
    INSERT INTO unidad (
        vin,
        nii,
        marca,
        modelo,
        anio,
        motivo_baja,
        id_tipo_unidad,
        id_estatus_unidad
    )
    VALUES (
        #{vin},
        #{nii},
        #{marca},
        #{modelo},
        #{anio},
        NULL,
        #{idTipoUnidad},
        #{idEstatusUnidad}
    )
</insert>

<update id="editar" parameterType="pojo.Unidad">
    UPDATE unidad
    SET
        marca = #{marca},
        modelo = #{modelo},
        anio = #{anio},
        nii = #{nii},
        id_tipo_unidad = #{idTipoUnidad},
        id_estatus_unidad = #{idEstatusUnidad},
        motivo_baja = #{motivoBaja}
    WHERE id_unidad = #{idUnidad}
</update>

<update id="dar-baja" parameterType="map">
    UPDATE unidad
    SET
        id_estatus_unidad = 2,
        motivo_baja = #{motivoBaja}
    WHERE id_unidad = #{idUnidad}
</update>

<select id="buscar-unidad" parameterType="map" resultType="pojo.Unidad">
    SELECT 
        u.id_unidad        AS idUnidad,
        u.vin,
        u.nii,
        u.marca,
        u.modelo,
        u.anio,
        u.motivo_baja,
        u.id_tipo_unidad   AS idTipoUnidad,
        tu.nombre          AS tipoUnidad,
        u.id_estatus_unidad AS idEstatusUnidad,
        eu.nombre          AS estatusUnidad
    FROM unidad u
    INNER JOIN tipo_unidad tu ON u.id_tipo_unidad = tu.id_tipo_unidad
    INNER JOIN estatus_unidad eu ON u.id_estatus_unidad = eu.id_estatus_unidad
    <where>
        <if test="vin != null and vin != ''">
            u.vin = #{vin}
        </if>

        <if test="marca != null and marca != ''">
            AND u.marca LIKE CONCAT('%', #{marca}, '%')
        </if>

        <if test="nii != null and nii != ''">
            AND u.nii LIKE CONCAT('%', #{nii}, '%')
        </if>
    </where>
</select>

<select id="obtener-tipos-unidad" resultType="pojo.TipoUnidad"> 
    SELECT id_tipo_unidad AS idTipoUnidad, 
    nombre AS nombre 
    FROM tipo_unidad 
</select>





</mapper>

